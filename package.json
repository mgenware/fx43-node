{
  "name": "fx43",
  "version": "0.0.10",
  "description": "Detecting changed files via caching file modification time",
  "main": "dist/lib/main.js",
  "types": "dist/lib/main.d.ts",
  "author": "Mgen (https://www.mgenware.com)",
  "homepage": "https://github.com/mgenware/fx43",
  "bugs": "https://github.com/mgenware/fx43/issues",
  "keywords": [
    "file changes",
    "file system"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mgenware/fx43.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/globby": "^6.1.0",
    "@types/mocha": "*",
    "@types/node": "*",
    "@types/tmp": "^0.0.33",
    "copy-dir": "^0.3.0",
    "copyfiles": "*",
    "mocha": "*",
    "tmp": "^0.0.33",
    "tslint": "^5.7.0",
    "typescript": "*"
  },
  "scripts": {
    "test": "yarn run clean && yarn run compile && yarn run lint && yarn run copy && mocha \"dist/test/*.js\"",
    "dev_test": "mocha \"dist/test/*.js\"",
    "build": "yarn test",
    "compile": "tsc",
    "dev": "yarn run clean && tsc -w",
    "prepublishOnly": "yarn run test",
    "lint": "tslint --project .",
    "copy": "copyfiles \"test/data/**/*.*\" \"dist\"",
    "clean:dist": "rm -rf dist",
    "clean": "npm run clean:dist"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "dependencies": {
    "globby": "^6.1.0",
    "mkdirp-promise": "^5.0.1",
    "write": "^1.0.3"
  }
}
